{
  "name": "scss",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "sass":
      "node-sass -o dist --output-style expanded --include-path src src/test.scss",
    "watch:sass": "nodemon -x 'npm run sass'"
  },
  "dependencies": {
    "node-sass": "^4.13.1",
    "nodemon": "^1.12.1"
  }
}
